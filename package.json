{
  "name": "meguidnafen",
  "version": "0.1.1",
  "description": "Simple Electron UI for Mednafen — select, import and run games.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . meguidnafen --platform=darwin,win32,linux --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^37.2.6"
  }
}
